import React from "react";

function AddPost() {


    return (
        <div className="flex items-center justify-center h-screen">
            <p className="text-white text-3xl">Add Post</p>
        </div>
    );
}

export default React.memo(AddPost);